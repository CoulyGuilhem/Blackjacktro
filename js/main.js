// ============================================================
//  MAIN — Boot sequence
// ============================================================

document.addEventListener('DOMContentLoaded', () => {
  Renderer.init();
  View.updateAll();
  console.log('BlackJack Balatro Edition — chargé.');
});
